// Supabase project settings are public client configuration, not passwords.
// Replace both values after running supabase/schema.sql in your free project.
export const FREE_BACKEND = {
  url: "YOUR_SUPABASE_URL",
  anonKey: "YOUR_SUPABASE_ANON_KEY",
};
